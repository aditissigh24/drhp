# Upstream (social-media-surveillance-ui) auth API. The OTP endpoints the
# ported sign-in flow calls: api/v1/auth/send-otp, login-otp, register/{otp},
# forgot-password, reset-password.
NEXT_PUBLIC_API_URL="https://drhp-beta-api.modusai.net/"
